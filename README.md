# SPS Master Control public information

This public repository contains only the SPS Master Control OAuth information page and privacy policy.

It intentionally contains no application source, Google resource identifiers, credentials, owner data, operational evidence, or downloadable application package.

Run `node verify-site.mjs` before publishing a change.
